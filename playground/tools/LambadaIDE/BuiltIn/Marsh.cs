using LightModel;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Reflection;
using System.Text;
using System.Threading.Tasks;

namespace LambadaIDE.BuiltIn
{
    /*public static class Marsh
    {
        public object Unmarsh(Type t, Node n)
        {
            
        }

        public T Unmarsh<T>(Node n)
        {
            return (T)Unmarsh(typeof(T), n);
        }

        // primitives
        public Node Marshal(uint i)
        {
            return new NatNumber(i).AsNode();
        }
        public Node Marshal(bool b)
        {
            return b ? Bool.TrueX : Bool.FalseX;
        }
        public Node Marshal(string s)
        {
            return List.FromString(s);
        }

        public Node Marshal(object self, MethodInfo method)
        {
            Node current = 

            var pms = method.GetParameters();
            if (pms.Length == 0)
                return Marshal(method.Invoke(self, new object[] { }));


        }

        public Node Marshal(object o)
        {
            
        }
    }*/
}
